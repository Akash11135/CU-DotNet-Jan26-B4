@model Vagabond_frontend.DTOs.DestinationDto

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4">

        <div class="card-header bg-success text-white rounded-top-4">
            <h4 class="mb-0">➕ Add New Destination</h4>
        </div>

        <div class="card-body">
            <form asp-action="Create">

                <div class="row g-3">

                    <div class="col-md-6">
                        <label class="form-label">Country</label>
                        <input asp-for="Country" class="form-control" placeholder="Enter country" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">City Name</label>
                        <input asp-for="CityName" class="form-control" placeholder="Enter city" required />
                    </div>

                    <div class="col-md-12">
                        <label class="form-label">Description</label>
                        <textarea asp-for="Description" class="form-control" rows="3" placeholder="Optional..."></textarea>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Rating (1-5)</label>
                        <input asp-for="Rating" type="number" class="form-control" min="1" max="5" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Last Visited</label>
                        <input asp-for="LastVisited" type="date" class="form-control" />
                    </div>

                </div>

                <div class="mt-4 d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary">⬅ Back</a>
                    <button type="submit" class="btn btn-success">Save Destination</button>
                </div>

            </form>
        </div>
    </div>
</div>