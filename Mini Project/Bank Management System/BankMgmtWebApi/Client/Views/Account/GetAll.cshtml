
@model List<Client.DTOs.AccountDto>



@{
    ViewData["Title"] = "All Accounts";
}


<div class="page-box">
<h1 class="page-title">Accounts</h1>
    <div class="mb-2">
        <a class="btn btn-dark" asp-controller="Account" asp-action="Create">Create Account</a>
    </div>
    <table class="table table-dark table-hover">
        <thead>
            <tr>
                <th>SNo</th>
                <th>Name</th>
                <th>Account Number</th>
                <th>Balance</th>
                <th>Acc Id</th>
                <th>History</th>
                <th>Add Transaction</th>
                <th>Actions</th>

            </tr>
        </thead>
        @{
            int cnt = 1;
        }
        <tbody>
            @foreach(var account in Model)
            {
                <tr>
                    <td>@cnt</td>
                    <td>@account.Name</td>
                    <td>@account.AccountNumber</td>
                    <td>Rs. @account.Balance</td>
                    <td>@account.Id</td>
                    <td>
                        <a class="btn btn-success" asp-controller="Transaction" asp-action="GetAllTransactions" asp-route-id=@account.Id>Transactions</a>
                    </td>
                    <td>
                       <a class="btn btn-light" asp-controller="Transaction" asp-action="Create" asp-route-id=@account.Id>+ New Transaction</a>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-controller="Account" asp-route-id=@account.Id class="btn btn-outline-secondary p-1 m-1">Edit</a>
                        <a asp-action="Details" asp-controller="Account" asp-route-id=@account.Id class="btn btn-outline-info p-1 m-1">Details</a>
                        <a class="btn btn-outline-danger p-1 m-1" asp-action="Delete" asp-route-id=@account.Id
                        >Delete</a>
                    </td>
                </tr>
                cnt++;  
            }
        </tbody>
</table>

</div>
